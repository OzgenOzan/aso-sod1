# Pinned dependencies for the ASO-SOD1 pipeline (Python 3.12)
#
# NOTE: These exact pins are RECONSTRUCTED (no pip freeze from the original
# environment was committed). Versions were chosen to satisfy the floor
# constraints in pipeline/outputs/web_tool/requirements.txt and to be
# mutually compatible on Python 3.12. If you have access to the original
# environment, replace these with its actual `pip freeze` output.
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
xgboost==2.1.3
lightgbm==4.5.0
shap==0.46.0
streamlit==1.40.2
openpyxl==3.1.5
torch==2.5.1
matplotlib==3.9.2
seaborn==0.13.2
scipy==1.14.1
